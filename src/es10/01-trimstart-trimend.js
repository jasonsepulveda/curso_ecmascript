// remove some space characteres at the begging or at the end of the strings

const hello = '        Hello world       ';
console.log(hello.trimStart());
console.log(hello.trimEnd());
console.log(hello.trim());